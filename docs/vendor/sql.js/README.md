# Vendored sql.js

Files here are copied from the [sql.js](https://github.com/sql-js/sql.js) npm
package (`sql.js` / MIT) for static GitHub Pages hosting.

Refresh with `npm run build:dictionary` (requires `npm install`).

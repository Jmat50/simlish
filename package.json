{
  "name": "simlish",
  "private": true,
  "type": "module",
  "scripts": {
    "build:models": "node scripts/sync-models.mjs",
    "build": "npm run build:models",
    "serve": "npx --yes serve docs -p 4173"
  }
}
